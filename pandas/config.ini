[dev]
    ports=27017
    host=<configure-server>
    password=<configure-password>
    user=<configure-username>
    database=<configure-database>
    collection_name=<configure-collection>

[prod]
    ports=27017
    host=<configure-server>
    password=<configure-password>
    user=<configure-username>
    database=<configure-database>
    collection_name=<configure-collection>